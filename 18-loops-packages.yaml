- name: Install packages using loops
  hosts: web1
  become: yes
  tasks:
  - name: Install packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: installed
    loop:
    - nginx
    - git
    - zip
