- name: nginx installation
  hosts: all
  become: yes
  serial: 10
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: installed
  - name: run nginx service
    ansible.builtin.service:
      name: nginx
      state: started
