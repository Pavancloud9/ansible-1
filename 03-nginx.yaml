- name: nginx
  hosts: web
  become: yes
  tasks:
  - name: Install nginx
    ansible.builtin.package:
      name: nginx
      state: absent
  # - name: start nginx service
  #   ansible.builtin.service:
  #     name: nginx
  #     state: disabled
  #     enabled: yes
    
